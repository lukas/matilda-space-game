{
  "name": "matilda-space-game",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:mobile": "playwright test --grep mobile"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
